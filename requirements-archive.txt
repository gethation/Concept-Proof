# Extras for the closed studies in scripts/archive/. Not needed to run anything
# reachable from make_reports.py.
#
#     conda run -n Quant pip install -r requirements-archive.txt

-r requirements.txt

# xgboost: 15m spread-change model, scripts/archive/ml/.
xgboost>=3.0,<4

# ib_async moved to requirements.txt when scripts/ingest/ibkr_umc.py made it a
# live ingest dependency rather than an archive-only one. The TAIFEX/US
# index-futures study in scripts/archive/us_index/download_legs.py still uses
# it and still guards the import.
